package com.marlowsoft.wofsolver.bind;

import com.google.inject.AbstractModule;

/**
 * Defines fake implementations of classes.
 */
public class MockWofModule extends AbstractModule {

    @Override
    protected void configure() {
        // TODO we should add a shortened version of the "real" on-disk dictionary here
    }
}
